
# VM Configuration
#
# 4 CPUs
# 4 GB Ram
# 250 GB Drive

sudo dnf -y update

# Start with turning on the minimize & maximize buttons
# From: https://bonguides.com/enable-minimize-and-maximize-buttons-on-almalinux-or-rocky-linux-8/
gsettings set org.gnome.desktop.wm.preferences button-layout ":minimize,maximize,close"

# Installed Rocky 10.2 as Workstation with Container Tools, so podman & buildah were already there.
sudo dnf -y install podman 
podman --version

# Test using:
# podman run hello-world

# Additional Podman Configuration
# Podman configurations can be found in the /etc/containers directory.
# Key files include:
#     policy.json:     For image signing policies
#     registries.conf: Lists available container image registries
#     storage.conf:    Configures defalt storage settings

# Skipped ahead to downloading with web browser from https://podman-desktop.io/downloads  >>>> Skip 
# Install Podman Desktop -- Flatpak steps continued to fail ?????
# flatpak is installed by default on Rocky 10, but can be installed using sudo dnf install flatpak
flatpak remote-add --if-not-exists --user flathub https://dl.flathub.org/repo/flathub.flatpakrepo

# If Podman Desktop fails, run:
flatpak install flathub org/freedesktop.Platform/x86_64/25.08

  flatpak install flathub io.podman_desktop.PodmanDesktop
# flatpak install --user flathub io.podman_desktop.PodmanDesktop
# -OR-

# Download from Podman.io, then     <<<<<< to here
cd Downloads
flatpak install --user podman-desktop-....
# But then had to run:
flatpak remote-add --if-not-exists --user flathub https://dl.flathub.org/repo/flathub.flatpakrepo
# Then redo the install step

# To run Podman Desktop:
Use OS start menu & type "Podma..." 

# To update Podman Desktop
flatpak update --user io.podman_desktop.PodmanDesktop

